apiVersion: v1
kind: Service
metadata:
  name: shottimer
  namespace: shottimer
  labels:
    app: shottimer
spec:
  selector:
    app: shottimer
  ports:
    - name: http
      port: 80
      targetPort: 8080
